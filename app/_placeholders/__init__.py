# RYAN-DAY1
"""Temporary placeholders for teammate-owned models — deleted on swap.

Owner: Ryan (placeholders only — NOT the real implementations).
Contains stand-ins for Samarth's Decision/Signal/Disposition (app/pdp/decision.py)
and Anamika's RequestContext (app/identity/context.py) so the PEP compiles and
runs before those modules land.
Gate: Day-1 noon. Import these via app._compat, never directly.
"""
