# Runtime dependencies for deploying the Sentinel PEP (fastapi app).
# These are the packages actually imported at runtime. Heavy/optional libs
# (presidio, pybloom, ahocorasick, asyncpg) are lazily imported with graceful
# fallbacks, so the app runs fully — including the 13 red-team behaviours —
# on just these five. Add the optional extras only if you want the upgraded
# detector backends.
fastapi>=0.114.0
uvicorn[standard]>=0.24.0
pydantic>=2.0
pyyaml>=6.0
httpx>=0.27          # app/escalation/queue.py posts Slack webhooks via httpx
